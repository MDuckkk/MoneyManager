# Money Manager OCR service — pipeline package.
# Các module dùng import phẳng (from common import ...); app.py/warmup.py thêm thư mục
# này vào sys.path (hoặc đặt PYTHONPATH=.../services trong Docker).
